  <%= f.input :code, :include_blank => '- не выбрано -', :wrapper_html => { :class => 'normal_width' } %>
  <%= f.input :tuitions, :as => :check_boxes, :collection => Intake.tuition.options %>
  <%= f.association :entrance_exams, :as => :check_boxes, :collection => @plan.entrance_exams %>

  <%= f.link_to_remove 'удалить' %>
