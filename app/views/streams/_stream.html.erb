<li>
  <%= stream %>
  <ul>
    <% stream.degrees.each do |degree| %>
      <li>
        <%= degree %>, <%= link_to 'Редактировать', edit_degree_path(degree) %>
      </li>
    <% end %>
  </ul>
</li>
