  <%= f.input :title %>
  <%= f.input :density %>
  <%= f.input :passing_grade %>
  <%= f.association :department, :collection => @plan.faculties, :as => :grouped_select, :group_method => :departments, :include_blank => '- не выбрано -' %>

  <%= f.link_to_remove 'удалить' %>
